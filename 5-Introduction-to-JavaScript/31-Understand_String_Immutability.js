// I learned that you can't change the values inside a string literal, you have to actually replace that string literal instead.

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
