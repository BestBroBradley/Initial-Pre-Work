// I learned that Math.random() will return a random fraction between 0 and 1

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
