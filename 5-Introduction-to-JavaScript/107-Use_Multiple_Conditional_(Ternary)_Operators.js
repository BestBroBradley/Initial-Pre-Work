// I learned that you can use a conditional operator to replace multiple connected if else statements

function checkSign(num) {

 return (num > 0) ? "positive" : (num < 0) ? "negative" : "zero";

}

checkSign(10);
