// I learned that you can use parseInt to analyze a string and return it as either an integer or NaN (not a number)

function convertToInteger(str) {
 return parseInt(str);
}

convertToInteger("56");
