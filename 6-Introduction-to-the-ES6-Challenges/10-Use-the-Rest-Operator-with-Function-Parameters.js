// I learned that you can use the rest operator (...) to allow a function to have an unspecified number of arguments.

const sum = (function() {
  "use strict";
  return function sum(...args) {
    return args.reduce((a, b) => a + b, 0);
  };
})();
console.log(sum(1, 2, 3)); // 6
